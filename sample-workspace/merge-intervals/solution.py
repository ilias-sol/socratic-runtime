"""
@socratic-task

Merge every pair of overlapping closed intervals.

Requirements:
- Accept intervals in arbitrary order.
- Treat touching endpoints as overlapping.
- Return intervals ordered by their starting value.
- Do not mutate the caller's nested lists.
- Handle empty input and duplicate intervals.
"""


def merge_intervals(intervals: list[list[int]]) -> list[list[int]]:
    pass
