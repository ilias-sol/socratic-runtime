"""
@socratic-task

Count the islands in a rectangular grid of "1" land and "0" water cells.

Requirements:
- Cells connect only through shared horizontal or vertical edges.
- Count every connected land region exactly once.
- Leave the caller's grid unchanged.
- Handle empty grids, narrow grids, and large connected regions.
- Avoid recursion-depth failures on long regions.
"""


def num_islands(grid: list[list[str]]) -> int:
    pass
